queue_rules:
  - name: default
    conditions:
      - status-check=build-and-test

pull_request_rules:
  - name: merge using the merge queue
    conditions:
      - and:
          - base=main
          - label=ready-to-merge
          - status-check=build-and-test
    actions:
      queue:
        name: default
